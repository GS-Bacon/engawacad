//! NOTE: 本ファイルは Issue #215 STEP 5.5 で作成されたスケルトン。
//! STEP 6 で GLM が実装した結果、テストの実体はすべて `face_entity_ref_planeref.rs`
//! (拡張子に _acceptance 無し) に書かれている。両ファイルを残すと「同名スケルトンが
//! #[ignore] + todo!() のまま残存」状態を pre-step8-check が偽陽性とみなすため、
//! スケルトン側は意図的に空にした。テスト計画 ID (T01〜T11) の検証は
//! `face_entity_ref_planeref.rs` 側で実施済み。
