1. [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:41) と [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:59) は「親 datum が必要なら将来 `face_ref` / `entity_ref` で表現できる」と置いていますが、この代替は成立していません。[ADR-010](/home/bacon/engawacad/docs/decisions/010-sketch-input-model.md:80) は Phase 8 を「ユーザーが追加できる `RefPlane`」の拡張として置いており、[ADR-005](/home/bacon/engawacad/docs/decisions/005-topological-naming.md:39) は `EntityRef` の参照スコープを単一 Component 内に限定しています。`face_ref` は実体化済みトポロジーを指すもので、抽象 datum や「子の最初の feature が依存する assembly datum」の代用品ではありません。

2. [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:40) と [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:56) は stdlib 再利用を Option B の根拠にしていますが、ここは設計階層を混同しています。[ADR-007](/home/bacon/engawacad/docs/decisions/007-assembly-and-references.md:57) は再利用部品を `ComponentRef` で別 Document の `root_component` として展開する設計で、再利用部品の datum 汚染問題は参照境界で既に切れています。実際の [assembly サンプル](/home/bacon/engawacad/examples/assembly.engawa:5) でも inline child と stdlib ref は別の役割です。inline child の意味論を「stdlib を inline したら困る」で決めるのは、assembly-local な datum 需要を不当に捨てています。

3. [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:55) の「parser-level 不変」は強い根拠ではありません。[Document::new/from_yaml](/home/bacon/engawacad/crates/engawa-format/src/document.rs:39) が canonical three を補完するのは root だけで、child は format 層では空のままです。しかも [Component.ref_planes](/home/bacon/engawacad/crates/engawa-format/src/component.rs:141) と [is_default_canonical_three](/home/bacon/engawacad/crates/engawa-format/src/ref_plane.rs:68) は empty と canonical を主にシリアライズ省略の都合で同一視しています。child の canonical 化は結局 [build 層](/home/bacon/engawacad/crates/engawa-build/src/lib.rs:494) が後付けで意味付けしているだけで、wire-format の正規化をそのままモデル意味論に昇格させています。

refute理由: 本 draft は「assembly-level datum」と「part-local canonical plane」を同一レイヤで潰しており、ADR-010 が Phase 8 の拡張点として置いた `RefPlane` 抽象を先に狭めています。しかも代替として挙げる `face_ref` / `entity_ref` は ADR-005 でも単一 Component 内参照が前提で、抽象 datum や子の初回スケッチ基準面の共有を表せません。さらに stdlib 再利用の議論は ADR-007 の `ComponentRef` 層で既に解かれている問題で、inline child にまで同じ独立性を強制する根拠になっていません。root の parser 正規化だけを child 意味論の根拠にしている点も層が逆転しており、Phase 8 の任意 RefPlane 拡張や Phase 17 の Mate/Joint 系で assembly datum を再導入したくなったときに手戻りが大きいため、as-is では採用不能です。
verdict: refuted