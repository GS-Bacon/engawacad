ADR-005 は `EntityRef` の参照スコープを「現在の文書の単一 Component 内」に限定しており、component/occurrence path は将来拡張の予告に留まります (`docs/decisions/005-topological-naming.md`)。実装もそれに従っていて、`EntityRef` には component path がなく、`feature_id` の slash は拒否され、`build_bodies_from_features()` の `plane_ref: Entity` 解決はその Component 内で build 済みの `built.live()` しか見ません (`crates/engawa-format/src/feature.rs`, `crates/engawa-build/src/lib.rs`)。それなのに本 ADR は「親 datum を child から参照したい用途は Phase 8/9 の `face_ref` / `entity_ref` で表現できる」として Option B の欠点を軽減可能と述べており、ここが過去 ADR と整合していません。さらに ADR-010 は RefPlane を Phase 8 の面由来スケッチの拡張点として採用済みで、assembly-scope datum の代替抽象を追加しないまま Option B を `Accepted` にすると、アセンブリ起点の datum を各 child の重複宣言へ押し込み、設計階層を「assembly datum」ではなく「part-local copy」の方向へ固定します。ADR-007 は transform 適用の決定であって datum scope 分離の根拠ではなく、むしろ world-coordinate B-rep 化は将来の assembly-level 参照を支える土台です。したがって Option B を恒久決定として accept するには、少なくとも assembly-level datum 抽象の予約、または ADR-005 の cross-component 参照拡張とセットの整理が先です。  
verdict: refuted