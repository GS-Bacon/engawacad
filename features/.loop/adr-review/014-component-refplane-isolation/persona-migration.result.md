1. `[ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:55)` の「parser-level 不変との整合」は成立していません。実装で canonical three を自動補完しているのは root だけで、`Document::new` / `from_yaml` / `Deserialize` は `root_component.ref_planes` しか触っていません [document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:39) [document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:69) [document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:96)。一方 child は `Component::new()` でも空のままです [component.rs](/home/bacon/engawacad/crates/engawa-format/src/component.rs:153)。それなのに公開コメントと生成 TS は「省略時は canonical three」と言い続けています [component.rs](/home/bacon/engawacad/crates/engawa-format/src/component.rs:141) [main.rs](/home/bacon/engawacad/crates/xtask/src/main.rs:1058)。つまり Option B は build-time fallback にすぎず、parser/schema 契約の後方互換を説明していません。

2. 既存設計断片との互換性整理が不足しています。Phase 7 の実装計画では `plane_ref: Some(id)` を `root_component.ref_planes` で解決する前提が明記されています [plan.md](/home/bacon/engawacad/features/158-format-refplane/plan.md:77)。ADR-014 はこれを child-local fallback に狭め、親 custom datum を child から見せない仕様に確定しています [ADR-014](/home/bacon/engawacad/docs/decisions/014-component-refplane-isolation.md:37) [lib.rs](/home/bacon/engawacad/crates/engawa-build/src/lib.rs:498)。これは意味論の変更ですが、`schema_version` は依然 1 のままです [document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:9)。既存 `.engawa` の scan、警告、version bump、移行規則のどれも無いので、「既存実装からの移行容易性」を満たしていません。

3. `既存実装・既存テスト整合` も後方互換の証拠としては弱いです。ADR が根拠にしている回帰テストは T15/T16/T17 ですが、どれも local custom plane または canonical `Front` の build 成功だけを見ており、root 側 custom datum を nested child が参照する既存文書の互換性は検証していません [refplane_acceptance.rs](/home/bacon/engawacad/crates/engawa-build/tests/refplane_acceptance.rs:301) [refplane_acceptance.rs](/home/bacon/engawacad/crates/engawa-build/tests/refplane_acceptance.rs:368) [refplane_acceptance.rs](/home/bacon/engawacad/crates/engawa-build/tests/refplane_acceptance.rs:452)。さらに referenced subtree は `Document::from_path()` 経由で root canonical 化される一方、inline child は build 時まで空のままなので、同じ意味の部品でも in-memory 表現が揃いません [document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:76) [lib.rs](/home/bacon/engawacad/crates/engawa-build/src/lib.rs:499)。inline/reference 間の移行互換まで未定義です。

refute理由: この ADR は「現行コードがそうなっているから移行コストはほぼ無い」と扱っていますが、後方互換の論証が欠けています。child `ref_planes` は parser/serde では canonical 化されず、build 時だけ fallback される一方で、公開コメントと TS 生成物は全 Component で canonical default があるかのように見せています。さらに Phase 7 の設計断片には `plane_ref` を `root_component.ref_planes` で解決する前提が残っており、ADR-014 はそれを `schema_version: 1` のまま child-local semantics に変更しています。既存文書の scan、warning、version bump、nested roundtrip/golden test が無い以上、「既存実装からの移行容易性 / 後方互換性」の観点では承認不可です。
verdict: refuted