1. `F01` 再生成ループが現行 L-5.6 に接続されていません。[loop-adr-pause-detector.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-pause-detector.ts:50) は `git log --diff-filter=A` で「新規追加 ADR」しか拾わず、auto-review も gate Issue 起票直後にしか呼びません（185-193 行）。一方 [loop-adr-auto-accept.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-auto-accept.ts:179) は refute 時に `regen_required` を返すだけで gate を残します（179-191 行）。その gate は [loop-should-stop.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-should-stop.ts:20) により次サイクルで全体 pause 条件になります（98-112 行）。つまり「cap 3 まで再生成」は draft 上の期待であって、既存実装からの移行経路がありません。最初の 1 回 refute された時点で止まります。

2. `F02` `needs-human` の意味を流用しており、既存の人手復旧フローと後方互換ではありません。[loop-failure-tracker.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-failure-tracker.ts:63) では `needs-human` は「実装失敗 3 回」の意味で、復旧は `reset --issue` が前提です。runbook も [docs/3ailoop-runbook.md](/home/bacon/engawacad/docs/3ailoop-runbook.md:114) で `features/.loop/failure-streak/<N>.json` を見る運用になっています。ところが [loop-adr-regen-tracker.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-regen-tracker.ts:116) の `retire()` は別状態 `features/.loop/adr-regen-count/<slug>.json` を使い、`gate:adr-review` を外して同じ `needs-human` を付けるだけです（123-127 行）。解除コマンドも対になる reset もありません。さらに dashboard は [loop-dashboard.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-dashboard.ts:141) で failure-streak しか読まないため、ADR 退避が「failures: 0」の通常失敗に見えます。運用互換が壊れています。

3. `F03` unresolved ADR を抱えたまま Phase 完了できてしまいます。[loop-phase-close-check.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-phase-close-check.ts:154) は `gate:human-feel` と `gate:adr-review` だけを完了阻害要因にしています（161-169 行）。ADR draft どおり cap 超過時に [loop-adr-regen-tracker.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-regen-tracker.ts:123) が gate を外して `needs-human` に変えるなら、その ADR は未解決でも Phase close 判定から消えます。これは現行の「ADR 判断が残っている間は phase を進めない」という振る舞いを後方非互換に崩します。特に foundation/docs 系 ADR は milestone の `type:feature` 完了条件にも乗らないため、すり抜けやすいです。

4. `F04` `dispatch-glm.ts --mode adr-review` は既存実装に対する自然な拡張点ではありません。[dispatch-glm.ts](/home/bacon/engawacad/.claude/skills/3ai/scripts/dispatch-glm.ts:142) は `--agent --plan-file --feature-dir --result-file` 必須の「実装ワーカー起動器」で、最後に常に `cargo xtask ci` を走らせる設計です（225-285 行）。既存のレビュー人格は別物として [dispatch-glm-review.ts](/home/bacon/engawacad/.claude/skills/3ai/scripts/dispatch-glm-review.ts:1) に分かれています。つまり ADR draft が書く「`dispatch-glm.ts` に mode 追加」は、現行コードから見ると小改修ではなく責務の再分配です。既存 caller とテストの互換性コストを無視しており、移行容易性の評価が甘すぎます。

以上より、この ADR は「自動 accept にする」という判断以前に、現行 `/3ailoop` の停止条件・再試行条件・人手復旧条件へ整合的に移行できる形で定義されていません。refute 後の再生成経路が存在せず、`needs-human` を既存意味と衝突させ、さらに unresolved ADR を `gate` から外すことで phase 完了条件まで変質させています。加えて GLM 連携面の指定も既存の dispatcher 境界と噛み合っていません。これは実装詳細の不足ではなく、後方互換な状態遷移が ADR 本文の時点で欠けているための構造欠陥です。

verdict: refuted