**Findings**
- Critical: [loop-adr-auto-accept.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-auto-accept.ts:330), [loop-adr-pause-detector.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-pause-detector.ts:175), [docs/3ailoop-runbook.md](/home/bacon/engawacad/docs/3ailoop-runbook.md:108), [final-review.yaml](/home/bacon/engawacad/features/237-phase9-adr-adr-draft-crud/final-review.yaml:12): 既存運用の `gate:adr-review` は「accepted = ADR の `Status` を `Accepted` に更新して commit してから gate を閉じる」という契約です。ところが本 ADR の auto-accept は gate 削除と Issue close しか定義しておらず、`Status` 遷移の互換仕様がありません。closed になった gate Issue の意味が旧運用と変わり、運用者にも自動化にも後方互換ではありません。しかもこの齟齬は既に `Status: Accepted` 先行で critical finding として検出されています。
- Critical: [SKILL.md](/home/bacon/engawacad/.claude/skills/3ailoop/SKILL.md:66), [loop-should-stop.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-should-stop.ts:20), [SKILL.md](/home/bacon/engawacad/.claude/skills/3ailoop/SKILL.md:137), [state.json](/home/bacon/engawacad/features/.loop/state.json:329): `loop-should-stop` は L-2 で `gate:adr-review` と `needs-human` を非 actionables として pause 判定しますが、ADR の rescan / auto-accept は L-5.6 です。つまり refute された ADR を「次サイクルで再生成」に回す経路が scheduler 上は保証されていません。実際に現在の state でも open gate が理由で loop が pause しています。旧 gate pause を安全に置換する移行設計になっていません。
- High: [loop-adr-regen-tracker.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-adr-regen-tracker.ts:116), [loop-phase-close-check.ts](/home/bacon/engawacad/.claude/skills/3ailoop/scripts/loop-phase-close-check.ts:155): cap 超過時は unresolved ADR を `gate:adr-review` から `needs-human` に変換しますが、phase 完了判定は `gate:human-feel` / `gate:adr-review` しか見ません。結果として、旧運用では phase を止めていた未決 ADR が、新運用では単なる backlog 扱いになり得ます。これは「ADR review が downstream phase 進行の hard gate である」という既存の後方互換性を破ります。

以上により、本 ADR は「人間 gate を自動化する」案ではなく、既存の `gate:adr-review` / `Status` / phase-close 契約を別の意味に置き換えてしまっています。closed gate がもはや「ADR 本文が Accepted に固定された」ことを保証せず、refute された draft も現行 scheduler では再生成経路が保証されず、cap 超過時には hard gate ですらなくなります。既存実装・runbook・運用ログが `gate:adr-review` を停止条件かつ整合性条件として使っている以上、少なくとも 1) `Status` 遷移の互換仕様、2) refute 時に必ず再生成へ進める scheduler 側の移行、3) `needs-human` 化しても phase を閉じさせない互換ルール、の 3 点が明文化されない限り、この ADR は移行容易性と後方互換性を満たしていません。

verdict: refuted