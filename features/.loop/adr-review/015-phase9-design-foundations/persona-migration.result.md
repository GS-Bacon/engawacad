- `high` CRUD 抽象が既存実装の保存的移行になっていません。現行は pure-functional な `FeatureCrud::{insert,edit,rollback,suppress,reorder,delete}` が `&Document` を受けて新しい `Document` を返す契約で、決定性テストもそれを前提にしています。[feature_crud.rs](/home/bacon/engawacad/crates/engawa-build/src/feature_crud.rs:839), [feature_crud_insert_acceptance.rs](/home/bacon/engawacad/crates/engawa-build/tests/feature_crud_insert_acceptance.rs:14) Draft の `FeatureOp::Edit { mutate: Box<dyn FnOnce(&mut Feature)> }` + `Document::apply_op() -> Result<(), BuildError>` は、CLI が現在行っている「Feature YAML を deserialize して丸ごと差し替える」流れと両立しません。[main.rs](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:235), [256_phase9_entry_edit_cli.rs](/home/bacon/engawacad/crates/engawa-cli/tests/256_phase9_entry_edit_cli.rs:37) `FnOnce` は serialize 不能かつ 1 回で消費されるため、ADR 自身が根拠にしている Undo/Redo の `inverse(&self)` にもそのまま乗らず、むしろ既存 API と別の edit 表現を増やすだけです。
- `high` 「CLI verb と enum variant が 1:1 対応する」という正当化も、現行 surface と噛み合っていません。今の CLI/build は `reorder(&doc, feature_id, before_id)` と `--before` を使う相対移動で、`--dry-run` は結果 YAML 全文を stdout に出す契約です。[feature_crud.rs](/home/bacon/engawacad/crates/engawa-build/src/feature_crud.rs:1001), [main.rs](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:112), [259_phase9_entry_reorder_cli.rs](/home/bacon/engawacad/crates/engawa-cli/tests/259_phase9_entry_reorder_cli.rs:36) ところが ADR の variant は `Reorder { new_index: usize }` と `component_path` を要求しており、既存の `before_id` ベース CLI/テスト群や root-only 実装からの移行方針がありません。`root_component.features` しか扱っていない現実装に対し、いきなり `component_path` を全 op 必須にするのは後方互換ではなく surface 変更です。[main.rs](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:50), [feature_crud.rs](/home/bacon/engawacad/crates/engawa-build/src/feature_crud.rs:840)
- `high` `schema_version` 節は migration ADR として未完成です。現行 loader は version を先読みした後、現在の `RawDocument` に typed deserialize し、ref_plane 補完と validation まで即実行します。[document.rs](/home/bacon/engawacad/crates/engawa-format/src/document.rs:72) 一方で `MigrationHook` 側は「自動適用 API はまだ無い」と明記しています。[migration.rs](/home/bacon/engawacad/crates/engawa-format/src/migration.rs:10) つまり実際に schema shape が変わったとき、hook を typed deserialize 前に掛けるのか、validation 前に掛けるのか、CRUD 実行後に `schema_version`/`version` を保持するのか bump するのかが未定です。現行 acceptance は `UnknownSchemaVersion` reject と YAML round-trip のみを保証しており、移行経路はまだ仕様化されていません。[schema_version_migration_acceptance.rs](/home/bacon/engawacad/crates/engawa-format/tests/schema_version_migration_acceptance.rs:31), [feature_crud_insert_acceptance.rs](/home/bacon/engawacad/crates/engawa-build/tests/feature_crud_insert_acceptance.rs:161)
- `medium` 品質基盤の記述も rollout 実態を過小評価しています。ADR は「5 ツールを CI で回せる最小 setup」と読めますが、現行ドキュメントは benchmark/fuzz を manual-only とし、workspace も fuzz crate を除外、CI も Playwright + `cargo xtask ci` までです。[QUALITY_TOOLS.md](/home/bacon/engawacad/docs/QUALITY_TOOLS.md:36), [Cargo.toml](/home/bacon/engawacad/Cargo.toml:11), [ci.yml](/home/bacon/engawacad/.github/workflows/ci.yml:34) これは互換性そのものより導入移行コストの見積り不備ですが、「まとめて入れる方が context switch が少ない」という採用理由を弱くしています。

Variable 2 段スコープ自体は、現行の `evaluate_scope()` が document→sketch の 2 段評価と sketch shadowing をすでに実装しているため、大きな blocker ではありません。[variable.rs](/home/bacon/engawacad/crates/engawa-format/src/variable.rs:48)

以上により refute です。この ADR は「移行基盤」を決める文書であるにもかかわらず、既存で安定している pure-functional CRUD 契約、CLI の YAML 入出力契約、root-only 操作面、そして `from_yaml` の現在のロード順序を壊さずにどう新抽象へ写像するかを定義していません。特に `Edit` を closure 化した案は現行のファイル駆動 CLI・決定性テスト・Undo の根拠と両立せず、`schema_version` も migration driver の適用位置が未定なままなので後方互換経路が仕様化されていません。Variable 部分だけではこの欠落を補えないため、保存的移行方針を先に固定しない限り承認不能です。

verdict: refuted