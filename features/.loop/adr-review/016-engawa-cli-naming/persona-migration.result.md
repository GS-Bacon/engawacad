1. `high` 既存 CLI の互換性評価の前提が誤っています。ADR は「既存 CLI は `run/convert`」として混在許容を論じていますが、現行の公開 surface は `export` / `view` / `entry` です。[ADR-016](/home/bacon/engawacad/docs/decisions/016-engawa-cli-naming.md:14) と [CLI 実装](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:20)、[README](/home/bacon/engawacad/README.md:27) が一致していません。この時点で「何を後方互換として守るのか」の inventory がずれており、「レガシーを残すので互換」は成立しません。

2. `high` canonical として固定している引数形と出力形式が、既存実装・テスト契約を破壊します。現行 `edit` / `rollback` / `suppress` / `restore` / `reorder` / `remove` は `feature_id` を positional で受けており、`--dry-run` は YAML 全文を `stdout` に出します。[CLI 実装](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:48), [edit test](/home/bacon/engawacad/crates/engawa-cli/tests/256_phase9_entry_edit_cli.rs:37), [reorder test](/home/bacon/engawacad/crates/engawa-cli/tests/259_phase9_entry_reorder_cli.rs:36), [suppress/restore test](/home/bacon/engawacad/crates/engawa-cli/tests/258_phase9_entry_suppress_cli.rs:27), [add test](/home/bacon/engawacad/crates/engawa-cli/tests/entry_add.rs:14)。一方 ADR は `--feature-id`、`--after`、標準 1 行サマリ、`--json`、`edit --feature-id box_1 --width 20` のような別 parser 形を canonical 化しています。[ADR-016](/home/bacon/engawacad/docs/decisions/016-engawa-cli-naming.md:47) これは alias 1 個で吸収できる差分ではなく、既存スクリプト・統合テストの破壊的変更です。

3. `medium` これは「実装前の命名規約」ではなく、既に存在する surface の改名 ADR になっています。各 Phase 9 issue は明示的に「ADR-016 確定を待たない」で実装を進めており、`entry` 配下の shape は先行確定済みです。[insert plan](/home/bacon/engawacad/features/255-phase9-feature-crud-insert/plan.md:8), [suppress plan](/home/bacon/engawacad/features/258-phase9-feature-crud-suppress/plan.md:9), [reorder plan](/home/bacon/engawacad/features/259-phase9-feature-crud-reorder/plan.md:7)。それなのに ADR は `restore` / `rollback` を代表一覧から落とし、`clap` のネスト構造を Open Question 扱いしていますが、実装はすでにネスト済みです。[CLI 実装](/home/bacon/engawacad/crates/engawa-cli/src/main.rs:41), [rollback test](/home/bacon/engawacad/crates/engawa-cli/tests/257_phase9_entry_rollback_cli.rs:57) 移行仕様として必要な「旧新両受理条件」「deprecation 期間」「対象コマンド全体の棚卸し」が不足しています。

この draft は命名方針の好み以前に、移行元の現状把握と後方互換計画が欠けています。現行 surface はすでに `export/view/entry`、positional `feature_id`、YAML を返す `--dry-run`、`restore`/`rollback` を前提にコード・README・統合テストが固まっており、ADR が固定する `run/convert` 前提、`--feature-id` canonical、1 行サマリ既定、`--json` はそのままでは既存利用者と自動テストを壊します。少なくとも旧新両受理、deprecation 期間、全コマンド inventory、出力互換の扱いが明文化されるまで、既存実装からの移行容易性と後方互換性の観点では承認できません。
verdict: refuted