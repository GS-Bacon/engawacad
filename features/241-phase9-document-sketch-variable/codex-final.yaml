# Merged Codex review (3 persona parallel, #231)
issues:
  - id: A-F01
    severity: critical
    file: "crates/engawa-format/src/lib.rs"
    line_hint: 7
    finding: "`pub mod variable;` / `pub use variable::{...}` を tracked diff に追加している一方で、新規 `crates/engawa-format/src/variable.rs` と `crates/engawa-format/tests/variable_acceptance.rs` が untracked のままです。`git diff` ベースでこの差分だけを適用すると `engawa-format::variable` モジュール不在でビルド不能になり、合意した Variable テスト群も差分に載りません。"
    suggestion: "新規 `variable.rs` と `variable_acceptance.rs` を VCS に追加し、レビュー対象の差分へ含める。"
  - id: A-F02
    severity: high
    file: "crates/engawa-format/src/variable.rs"
    line_hint: 181
    finding: "`eval_expr` が `parser.parse_expr()` の返り値をそのまま返しており、式末尾の未消費トークンを検査していません。このため `\"2 3\"` や `\"10foo\"` のような不正式が `EvalError::Parse` ではなく `2` / `10` として受理され、退化入力で誤値を静かに返します。"
    suggestion: "`parse_expr()` 後に空白を読み飛ばし、まだ文字が残っていれば `EvalError::Parse` を返す終端チェックを追加する。"
  - id: A-F03
    severity: high
    file: "crates/engawa-format/src/variable.rs"
    line_hint: 193
    finding: "`detect_cycle` が開始ノード集合を `HashSet` で反復しているため、複数の循環候補がある入力では返す `CircularDependency.cycle` が hash seed 依存になります。Issue #241 で最重要としている決定性と、plan で定めた lexicographic tiebreak の契約を満たせません。"
    suggestion: "cycle 検出の走査順を `effective` の順序付きキーかソート済み `Vec<String>` に固定し、`HashSet` / `HashMap` の反復順へ依存しない実装にする。"
  - id: C-F01
    severity: high
    file: "crates/engawa-format/src/variable.rs"
    line_hint: 117
    finding: "循環検出で `effective.keys()` を `HashSet` に落としてから DFS を開始しているため、`CircularDependency { cycle }` の開始点がプロセスごとに揺れます。T_DEG_circular で前提にしている lexicographic な決定性を満たせません。"
    suggestion: "`HashSet` ではなく `effective.keys()` の順序を保持した `Vec` / `IndexSet` / `BTreeSet` を使って DFS 開始順を固定し、cycle の返却順も決定的にする"
  - id: C-F02
    severity: high
    file: "crates/engawa-format/src/variable.rs"
    line_hint: 181
    finding: "`eval_expr` が `parser.parse_expr()` の結果をそのまま返しており、入力末尾の未消費トークンを検出しません。`\"1 2\"` や `\"10)garbage\"` のような不正式が先頭部分だけ評価されて成功扱いになります。"
    suggestion: "`parse_expr()` 後に `skip_whitespace()` を呼び、`peek_char().is_some()` なら `EvalError::Parse` を返して式全体を必ず消費させる"
  - id: C-F03
    severity: medium
    file: "crates/engawa-format/tests/variable_acceptance.rs"
    line_hint: 180
    finding: "T09 のコメントは `Document.variables + Sketch.variables` の YAML roundtrip を要求していますが、実際のテストは `doc.variables` しか設定しておらず、新規追加した `Feature::CreateSketch.variables` の永続化経路を通していません。合意済みテスト計画と実装がずれています。"
    suggestion: "`CreateSketch { variables: ... }` を含む `Document` を組み立てて roundtrip させ、Sketch 側 `variables` も byte-identical に保持されることを確認する"
  - id: M-F01
    severity: critical
    file: "web/src/generated/Document.ts"
    line_hint: 16
    finding: "`Document.variables` と `Feature.create_sketch.variables` で永続化スキーマを拡張していますが、diff 内に互換性テストや golden YAML 更新が一切ありません。`TEST SUMMARY` でも `total_added=0` かつ `golden=0` のため、既存 YAML の読込互換と新フィールドの round-trip が未検証です。"
    suggestion: "旧形式（variables なし）と新形式（Document/Sketch に variables あり）の YAML について deserialize/serialize round-trip と golden を追加し、後方互換を固定してください。"
  - id: M-F02
    severity: high
    file: "web/src/generated/Document.ts"
    line_hint: 1
    finding: "この差分は `web/src/generated/*.ts` の生成物だけを更新しています。これらは `ts-rs` の生成ファイルなので、source of truth である Rust 側の型定義変更が同一差分に無いまま here だけ変えると、次回 regenerate で消えるうえ、フロントエンド型だけが先行する API ドリフトになります。"
    suggestion: "Rust 側の `Document` / `Feature::CreateSketch` / `Variable` を更新してから `ts-rs` を再生成し、生成元の変更と生成結果を同じ差分に含めてください。"

verdict: fail
# severity_counts: critical=2, high=5, medium=1, low=0
# blocking: 7
