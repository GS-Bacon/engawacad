# Codex 3 persona findings (#273)

STEP 7.5 で merged verdict = pass (blocking 0)。以下は medium/low の non-block 指摘で、本 Issue 完了後の整地として別 Issue 起票するか合意の上で対応する。

**Note**: 本サイクルは Codex usage limit のため **#281 GLM 3 persona fallback ルート**で実行 (`fallback_used: true`)。各 finding は GLM が Codex persona role hint を受けて生成したもの。

## C-F01 (low) — TypeScript 側のテスト確認

> SketchSegment から SketchElement へのリネームは、既存の line-only ワークロードに対して破壊的変更を伴わないか検証が必要

**status**: 本 Issue 内で対応済。`web/src/sketch.test.ts` と `web/src/extrude.test.ts` を SketchElement 形式に migrate し、`bun run typecheck` + `cargo xtask ci` green を確認した。

## C-F02 (medium) — Arc CCW 規約の Rust 側整合

> Arc の start_angle/end_angle 定義で「CCW from +X axis」と明記されているが、math crate 側の実装と整合しているか検証が不足

**status**: 本 Issue 内で実装済。`crates/engawa-kernel/src/tessellation/sketch.rs` の `tessellate_sketch_element` で `[center[0] + radius * t.cos(), center[1] + radius * t.sin()]` を生成しており、これは +X 軸からの CCW (反時計回り) で一致。さらに `crates/engawa-kernel/tests/sketch_element_acceptance.rs::t02_arc` で Arc(0,0,r=5,start=0,end=π/2) が build できることを確認、 `t_boundary_full_circle` で Arc(0,2π)==Circle の整合を確認している。

## C-F03 (low) — Legacy YAML round-trip 追加テスト

> 「Tagged serialization with `kind` field」とあるが、untagged fallback の legacy YAML が実際に機能しているか検証不足

**status**: 本 Issue 内で実装済。`crates/engawa-format/tests/sketch_element_golden_acceptance.rs::t_golden_legacy_compat` で legacy YAML (`kind` なしの `{id, from, to}`) が `SketchElement::Line` として deserialize できることを確認。green を確認済。

## M-F01 (medium) — SketchSegment 削除の後方互換 (Rust API)

> SketchSegment type が削除されており、既存コードで `import { SketchSegment }` を使用している箇所でコンパイルエラー (後方互換性違反)

**status**: 本 Issue 内で対応済。
- Rust 側: 全 `crates/**/*.rs` で SketchSegment 残参照を 0 にした (round 2-3 で migration)。`grep -rn SketchSegment crates/` で 0 件
- TS 側: `web/src/generated/SketchSegment.ts` は削除、`web/src/generated/SketchElement.ts` を新設 (auto-generated by ts-rs)
- 手書き TS (`web/src/extrude.ts`, `sketch.ts`, テスト) は SketchElement の discriminated union (`kind: "line"` narrowing) に migration 済

Codex の指摘は YAML legacy 互換と Rust/TS API 互換を混同しているが、本 Issue は **YAML legacy 互換 (= `kind` 欠落で Line に default) を維持しつつ Rust/TS の型レベルでは breaking change** という方針 (ADR-017 §4)。これは plan.md と self-review.md でも明示している。

## Claude self-review (STEP 6.7) で identified された残課題

`claude-self-review.md` の以下 3 件は medium で受容済。フォロー Issue として整地する:

- **A01 (medium)**: `Deserialize` impl が YAML-only (`serde_yaml::Value` 経由) — JSON 経路では使えない。フォロー Issue で format-agnostic 化
- **M01 (medium)**: `examples/*.engawa` 3 件 (extruded_rect, sketch_via_refplane, two_bodies) が tagged 化された — legacy 形式の実証 example を 1 個残す判断は別 Issue
- **C01 (medium→受容)**: `kind: circle, ..., from, to` のような混在 YAML が silently 通る (ADR-017 §4 reject 戦略の精神的違反) — `deny_unknown_fields` 追加は別 Issue
