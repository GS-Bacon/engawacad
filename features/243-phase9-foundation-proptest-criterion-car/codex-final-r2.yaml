# Merged Codex review (3 persona parallel, #231)
issues:
  - id: A-F01
    severity: medium
    file: "crates/engawa-kernel/tests/cuboid_determinism_proptest.rs"
    line_hint: 29
    finding: "新規 proptest は 2 回生成した `Solid` の `Debug` 文字列比較しかしておらず、`validate_manifold()` や Euler-Poincare を確認していないため、同一入力で毎回同じ壊れた HalfEdge/Loop/Shell を返す回帰を通してしまう。境界ケース側も tessellation 成功しか見ていない。"
    suggestion: "生成した `Solid` ごとに `validate_manifold()` と `euler_poincare() == 0` を assert し、必要なら tessellation 結果も 2 回比較して決定性を下流成果物まで固定する"
  - id: A-F02
    severity: low
    file: "crates/engawa-format/fuzz/fuzz_targets/from_yaml.rs"
    line_hint: 3
    finding: "未使用の `CStr` import が残っており、この fuzz target 単体で `clippy -D warnings` を回すと警告で落ちる。"
    suggestion: "未使用 import を削除する"
  - id: C-F01
    severity: high
    file: "bench-results/baseline-phase9.json"
    line_hint: 1
    finding: "commit されている `phase9` baseline は Criterion が再利用できる baseline ではなく要約 JSON に留まっている。`docs/QUALITY_TOOLS.md` の `cargo bench -p engawa-kernel --bench tessellation -- --baseline phase9` は `target/criterion/.../phase9/` を要求するため、fresh clone では Phase 9 基準値比較が成立しない。"
    suggestion: "この JSON を参考スナップショット扱いにして docs を訂正するか、実際の Criterion baseline を materialize するスクリプト/配置を追加して `--baseline phase9` が再現可能になる形にする。"
  - id: C-F02
    severity: medium
    file: "crates/engawa-kernel/benches/tessellation.rs"
    line_hint: 8
    finding: "`tessellate_cuboid_10x20x30` の測定区間に `IdGenerator` 初期化と `make_cuboid` が含まれており、tessellation 回帰ではなく solid 構築＋tessellation の合算値を baseline 化している。"
    suggestion: "測定対象を `tessellate_solid` に絞るなら `solid` を `b.iter` の外で一度だけ作るか、fresh state が必要なら `iter_batched` で setup と測定を分離する。"
  # [migration] issues: [] (verdict=pass)

verdict: fail
# severity_counts: critical=0, high=1, medium=2, low=1
# blocking: 1
