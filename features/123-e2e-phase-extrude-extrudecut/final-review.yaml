issues:
  - id: FN01
    severity: high
    file: "web/tests/acceptance_extrude.spec.ts"
    line_hint: 18
    finding: "設計方針「各テストは独立した tmp ファイルでサーバを起動（並列競合なし）」と実装が矛盾。test.describe.configure({ mode: \"serial\" }) でシリアル実行され、共有サーバーを前提としているため --workers 20 の効果が得られない"
    suggestion: "setupPageWithServer() を使用して各テストで独立したサーバーを起動するようリファクタし、mode: \"serial\" を削除して並列実行を有効化する"

verdict: fail