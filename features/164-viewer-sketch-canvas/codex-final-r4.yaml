issues:
  - id: F01
    severity: high
    file: "web/src/viewer.ts"
    line_hint: 429
    finding: "`setSketchMode(false)` が常に `sketchOverlay.visible = false` を強制するため、`main.ts` の auto finalize 経路でループを閉じた直後に確定済みセグメントが消えます。`btn-start-sketch` 開始時に既存 overlay を clear している実装とコメントは「閉じた輪郭を次回開始まで残す」前提で、closed 完了物の可視フィードバックが壊れています。"
    suggestion: "OrbitControls の有効/無効と overlay 可視性を分離し、auto finalize では controls だけ戻して overlay は次の `btn-start-sketch` または `Escape` まで残す。"
  - id: F02
    severity: medium
    file: "web/tests/sketch_canvas.spec.ts"
    line_hint: 130
    finding: "合意済み T03/T04/T05 の受け入れ条件がまだ十分に検証されていません。T03/T11 は 5 点目を最初と同じピクセルへ再クリックしており `SNAP_RADIUS` 経由の close を通さなくても通過し、T04 は `controls.enabled`/segments 消去を、T05 は `finalize() === null` を実際には assert していません。これでは snap/Enter/Escape 経路の退行を取りこぼします。"
    suggestion: "Playwright から参照できる最小 debug hook を公開し、T03/T11 はオフセットクリックで snap close を、T04 は `window.__viewer.controls.enabled` と overlay/session の消去を、T05 は `finalize()` の戻り値 `null` または finalize ログ未発生をそれぞれ assert する。"

verdict: fail