issues:
  - id: F01
    severity: critical
    file: "web/playwright.config.ts"
    line_hint: 21
    finding: "実 API サーバーを source-controlled `examples/simple_box.mycad` に直接向けており、`acceptance_extrude.spec.ts` の POST がそのファイルを更新する。`cargo xtask acceptance` 実行でワークツリー汚染とテスト順序依存が発生する。"
    suggestion: "サーバー起動前に `simple_box.mycad` を temp dir へコピーし、その temp path を `mycad-api` に渡す。run/test ごとに独立した入力ファイルを使う。"
  - id: F02
    severity: high
    file: "crates/mycad-api/tests/fuzz_features.rs"
    line_hint: 280
    finding: "`NaN` / `Infinity` を含む生 JSON は構文エラーだが、テストが 422 固定になっている。Axum 側が正しく 400 Bad Request を返しても `cargo xtask acceptance --fuzz` が失敗する。"
    suggestion: "`StatusCode::BAD_REQUEST || StatusCode::UNPROCESSABLE_ENTITY`、または `status.is_client_error()` を受け入れるようにする。"
  - id: F03
    severity: high
    file: "web/tests/acceptance_extrude.spec.ts"
    line_hint: 81
    finding: "T01-T09 の検証が `200` / `\"error\"` なし / 非空 mesh に寄り過ぎており、押出・押出カットで実際に target 形状が変化したことを確認していない。既存 mesh を返す no-op や誤ターゲットでも通過しうる。"
    suggestion: "各ケースで事前に `/api/v0/mesh` を取得し、頂点数・body 数・face_id などの前後差を比較する。少なくとも T01-T03 と T06 は plan 通りの形状変化アサートを追加する。"

verdict: fail