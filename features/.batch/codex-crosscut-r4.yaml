issues:
  - id: F01
    severity: medium
    file: "web/src/main.ts"
    line_hint: 271
    finding: "スケッチ由来の `extrude` / `extrude_cut` 成功後に `lastFinalizedSketch` と Three.js overlay は消しているが、`[data-testid=\"sketch-canvas\"]` を `data-state=\"idle\"` へ戻していない。成功後も DOM 上は `\"closed\"` のまま残るため、『送信成功でスケッチセッションをリセットする』という UI 状態契約を破っている。"
    suggestion: "両方の成功経路で `handle.clearSketchOverlay()` と一緒に `updateSketchCanvasState()` を呼ぶか、`sketch-canvas` の `data-state` を明示的に `\"idle\"` へ戻す。"

verdict: pass