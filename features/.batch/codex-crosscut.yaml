issues:
  - id: F01
    severity: critical
    file: "crates/engawa-build/tests/refs_resolve_transitive_plane_ref_acceptance.rs"
    line_hint: 1
    finding: "Issue #269 用に追加した integration test ファイルに `#[test]` が1件もなく、実テストは既存スイート側の `t_269_*` / `EDGE_269_*` に置かれているため、添付 TEST SUMMARY では `coverage_hints.total_added=0`, `determinism=0`, `degenerate=0`, `golden=0` のままになっている。#269 は決定性と退化系を含むテスト追加が要件なので、この差分のままでは自動ゲート上は『テスト未追加』として扱われる。"
    suggestion: "Issue 専用ファイルに実テストを移すか、summary 抽出器が拾う命名/配置に揃えて `coverage_hints` に反映される形へ修正する。"
  - id: F02
    severity: high
    file: "crates/engawa-build/tests/feature_crud_prefix_validate_acceptance.rs"
    line_hint: 1507
    finding: "`t_269_determinism` と `t_269_edge_determinism_100_iter` はどちらも broken-prefix の `BodyNotFound` 一致しか見ておらず、合意済み T01 の『成功パスを2回実行して post-insert の結果順序を比較する』検証になっていない。加えて `t_269_clean_history_tail_insert` は既存 consumer が live のままかを確認せず、`t_269_clean_history_unaffected` は `CreateBox` しか通らないため、新規の transitive plane_ref check が clean history の成功パスで回帰してもこの suite では検出できない。"
    suggestion: "clean history に既存 `Extrude` / `ExtrudeCut` を含めた成功ケースで同じ `FeatureCrud::insert` を2回 `Ok` まで実行し、返却 `Document` の feature 順序または `to_yaml()` を byte-equal で比較するテストへ差し替える。T02/T03 も `refs_resolve_in_state` の transitive 分岐を実際に通す形に組み直す。"

verdict: fail