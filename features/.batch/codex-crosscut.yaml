issues:
  - id: F01
    severity: medium
    file: "crates/engawa-build/src/feature_crud.rs"
    line_hint: 267
    finding: "`check_self_reference` が `feature_sketch_refs` と direct な `feature_body_refs` しか見ておらず、今回追加した `CreateSketch.plane_ref = PlaneRef::Entity(...)` の implicit body ref を自己参照判定に含めていない。そのため `plane_ref` の `Named`/`Derived` leaf が自分自身の `id` を指す sketch は `SelfReference` ではなく後段で `BodyNotFound` として落ち、既存の自己退化エラー契約と新しい implicit ref 経路の意味論がずれる。"
    suggestion: "`check_self_reference` に `feature_implicit_body_refs(f)` も通し、`plane_ref` 経由で自分自身を参照する `CreateSketch` を `ref_kind: \"body\"` の `SelfReference` で拒否する回帰テストを追加する。"

verdict: pass