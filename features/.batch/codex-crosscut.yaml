issues:
  - id: F01
    severity: medium
    file: "docs/QUALITY_TOOLS.md"
    line_hint: 43
    finding: "`cd crates/engawa-format/fuzz` の直後に `cargo fuzz run from_yaml fuzz/corpus/from_yaml/` を案内しており、実際には `crates/engawa-format/fuzz/fuzz/corpus/from_yaml/` を参照して失敗する。manual corpus 実行手順が壊れている。"
    suggestion: "`cd` 後は `cargo fuzz run from_yaml corpus/from_yaml/` に修正するか、repo ルートから実行する前提の記述に統一する。"
  - id: F02
    severity: medium
    file: "crates/xtask/src/main.rs"
    line_hint: 1000
    finding: "ts-rs の回帰テスト更新が `Document.ts` と `Feature.ts` の golden 比較だけに留まっており、新規公開 DTO `web/src/generated/Variable.ts` を一度も検証していない。今回追加した生成ファイルが未生成・署名ドリフトしても CI で検出できない。"
    suggestion: "`Variable.ts` 用の golden 文字列と比較テストを `xtask` の既存生成物テストへ追加する。"

verdict: pass