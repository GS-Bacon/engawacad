issues:
  - id: F01
    severity: high
    file: "crates/mycad-kernel/tests/trim_sphere_circ_normal_acceptance.rs"
    line_hint: 760
    finding: "`t04_shared_boundary_with_cyl_lateral` が triangle 所有者の特定に `face.name.as_ref().map(...).unwrap_or_default()` を使っており、`Face.name == None` / 空名の face が複数あると `mesh.face_ids` 上で全部 `\"\"` に潰れます。すると strict 比較が対象の trimmed sphere face / 隣接 face 以外の triangle を混ぜてしまい、共有境界が壊れても偽陽性で通るため、この Issue の boundary strict 回帰を実際には固定できません。"
    suggestion: "tessellate 前に比較対象 face へ一意な test-local name を付けるか、`TriangleMesh` 側に face index 等の一意な所有者情報を伝搬して triangle 集合を曖昧なく引けるようにしてください。"

verdict: fail