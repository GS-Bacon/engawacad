issues:
  - id: F01
    severity: critical
    file: "features/269-engawa-build-refs-resolve-in/test-summary.json"
    line_hint: 1
    finding: "この差分でも `coverage_hints.total_added=0` かつ `coverage_hints.determinism=0` のままです。#269 は新規テスト追加と決定性検証が必須要件ですが、追加したテスト群が `t_269_*` 命名のため集計器に拾われず、受け入れゲート上は依然として未カバー扱いになっています。"
    suggestion: "新規テスト名を集計対象の形式（`test_*` または `t01_*` など）に揃えて TEST SUMMARY を再生成し、`coverage_hints.total_added` と `coverage_hints.determinism` が 0 でないことを確認する"

verdict: fail