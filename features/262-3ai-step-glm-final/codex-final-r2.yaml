# Merged Codex review (3 persona parallel, #231)
issues:
  - id: A-F01
    severity: high
    file: ".claude/skills/3ai/scripts/dispatch-glm-review.ts"
    line_hint: 82
    finding: "`detectDispatchFailure` は top-level `verdict:` 行の存在だけで成功扱いするため、`verdict: pass` だけの不完全出力を `blocking=0` の pass として通し、`verdict: fail` だけの出力も `blocking=0` / `verdict=fail` で STEP 7 のどの分岐にも入らない。『YAML 構造が成立していない出力を dispatch_error に倒す』という今回の契約をまだ満たしていない。"
    suggestion: "成功判定は `verdict:` だけでなく top-level `issues:` を含む完全なレビュー形状の検証に変え、少なくとも `issues` 欠落、および `verdict: fail` なのに issue 0 件の出力を `dispatch_error` に倒す回帰テストを追加する。"
  - id: C-F01
    severity: high
    file: ".claude/skills/3ai/scripts/dispatch-glm-review.ts"
    line_hint: 82
    finding: "`detectDispatchFailure` は `^verdict: pass|fail` の1行があるだけで transport 成功とみなしますが、その `verdict:` が本当にトップレベルのレビュー出力かを検証していません。final review の入力 diff 自体に `verdict:` 断片が含まれるため、モデルが入力片をそのまま引用しただけの壊れた出力でも `dispatch_error` を素通りし、`parseVerdict` がその断片を正規の判定として拾って再び偽 `pass` / `blocking=0` を作れます。"
    suggestion: "成功条件を「`verdict:` 行があること」ではなく「期待する YAML スキーマを正しく parse できること」に引き上げ、少なくとも root の `issues` と `verdict` を検証してください。あわせて、引用された `verdict:` 断片だけでは `dispatch_error` になる回帰テストを追加してください。"
  - id: C-F02
    severity: medium
    file: ".claude/skills/3ai/scripts/dispatch-glm-review.ts"
    line_hint: 322
    finding: "failure 分岐で保存しているのは `rawOut` だけで、`rawErr` は stderr に最大 500 文字流すだけです。非ゼロ終了系の障害は stderr 側にしか実情報が出ないことが多いため、保存された `final-review.yaml` が空のままになり、後続の dispatch_error 起票で真因を feature 配下の成果物から追えません。"
    suggestion: "`rawErr` も feature 配下に永続化してください。`resultFile` へ追記するか、隣に `.log` を書いて dispatch_error 起票時に参照させる形が安全です。"
  - id: M-F01
    severity: high
    file: ".claude/skills/3ai/scripts/dispatch-glm-review.ts"
    line_hint: 302
    finding: "`dispatch-glm-review.ts` は `dispatch-codex.ts` と違って旧 `resultFile` / `*.verdict.json` を事前削除せず、preflight の `process.exit(1)` や `main().catch(...)` でも error verdict を書きません。`final-review.yaml` を同じパスで再実行する運用では、`claude` spawn 例外や env/CLI 不備のときに前回の `final-review.verdict.json` が残り、STEP 7 が stale pass/fail を読んで今回追加した `dispatch_error` 分岐を素通りできます。"
    suggestion: "dispatch 開始前に `resultFile` と verdict.json を削除し、少なくとも top-level catch / preflight failure では `makeErrorVerdict(...)` を書いて fail-closed にしてください。"
  - id: M-F02
    severity: high
    file: ".claude/skills/3ai/scripts/dispatch-glm-review.ts"
    line_hint: 82
    finding: "`detectDispatchFailure()` は `^verdict:\\s*(pass|fail)` が 1 行でもあれば成功扱いにするため、トップレベル YAML を返せていない出力でも diff 由来の `verdict:` 行を引用しただけで `dispatch_error` を回避できます。今回の diff 自体に `codex-final*.yaml` の `verdict:` 行が多数含まれており、malformed 出力が `{verdict:\"pass\"|\"fail\", blocking:0}` に化けると #255 と同じ『blocking 0 だが正常でも error でもない』宙ぶらりん状態を再発させます。"
    suggestion: "成功条件を『抽出した本文が期待する YAML 形状を満たすこと』まで引き上げてください。少なくとも top-level `issues:` と `verdict:` の両方を確認するか YAML を実際にパースし、`non-YAML + quoted verdict line` の回帰テストを追加してください。"

verdict: fail
# severity_counts: critical=0, high=4, medium=1, low=0
# blocking: 4
