# Merged Codex review (3 persona parallel, #231)
issues:
  # [architect] issues: [] (verdict=pass)
  - id: C-F01
    severity: medium
    file: "crates/engawa-build/src/feature_crud.rs"
    line_hint: 897
    finding: "`rollback` は `truncate(idx)` の後に `Document::validate()` しか掛けておらず、残った prefix の feature 参照整合性を検証していない。`Document::from_path()` は forward ref や missing body を拒否しないため、不正な `.engawa` を `entry rollback` に通すと、surviving prefix が build 不能でも成功として書き戻してしまう。"
    suggestion: "返却前に surviving prefix 全体を `simulate_history` か既存の参照検証ロジックで再検証し、未解決 ref が残る場合はエラーにする。あわせて「不正 history を rollback したら reject する」退化テストを追加する。"
  - id: M-F01
    severity: medium
    file: "crates/engawa-cli/tests/257_phase9_entry_rollback_cli.rs"
    line_hint: 8
    finding: "CLI の公開面で in-scope とされた `--dry-run` 分岐が未テスト。追加された受け入れテストは `--output` 経路しか検証しておらず、stdout への YAML 出力や入力ファイルを上書きしないことの回帰を捕捉できない。"
    suggestion: "`t04_cli_rollback_dry_run` を追加し、`engawa entry rollback <input> <feature_id> --dry-run` の stdout が期待 YAML と byte-equal であること、および入力 `.engawa` が未変更であることを確認する。"

verdict: pass
# severity_counts: critical=0, high=0, medium=2, low=0
# blocking: 0
