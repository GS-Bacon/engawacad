issues:
  - id: A01
    severity: medium
    file: "crates/engawa-kernel/src/geometry/sketch_fillet.rs"
    line_hint: 57
    finding: "ゼロ長入力の判定が `len_a || len_b` を1本化しており、`elem_b` だけが退化している場合でも `DegenerateSketchElement.element_id` に常に `a_id` を返します。退化要素の特定が誤るため、呼び出し側の診断と退化テストが実入力とずれます。"
    suggestion: "`len_a` と `len_b` を個別に判定し、それぞれ `a_id` / `b_id` を返すよう分岐してください。あわせて `elem_b` のみゼロ長な回帰テストを追加してください。"
  - id: M01
    severity: medium
    file: "crates/engawa-build/tests/sketch_fillet_acceptance.rs"
    line_hint: 163
    finding: "`t08_cw_profile_negative_sweep` は前半で pure helper の負 sweep を確認していますが、後半の build 経路では `Feature::SketchFillet` を入れずに生の CW 矩形をそのまま `Extrude` しています。このため、テスト名と計画が要求している『負 sweep fillet が build まで通る』統合経路は実際には未検証です。"
    suggestion: "T08 の feature 列に `Feature::SketchFillet` を含めて、`SketchFillet -> tessellate_sketch_element -> make_extrusion` の実経路で build 成功を検証してください。"

verdict: pass